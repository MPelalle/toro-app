"use client";

import Link from "next/link";
import { useParams, useRouter } from "next/navigation";
import { ArrowLeft, Plus, Save, Trash2 } from "lucide-react";
import { useEffect, useState } from "react";
import { deleteRoutineOfflineFirst, getRoutineOfflineFirst, Routine, updateRoutineOfflineFirst } from "@/lib/routines";

const daysOfWeek = ["Lun", "Mar", "Mié", "Jue", "Vie", "Sáb", "Dom"];

export default function EditRoutinePage() {
  const { id } = useParams<{ id: string }>(); const router = useRouter();
  const [routine, setRoutine] = useState<Routine | null>(null); const [error, setError] = useState("");
  useEffect(() => { let active = true; void getRoutineOfflineFirst(id).then((result) => { if (active) setRoutine(result.routine ?? null); }).catch((cause) => { if (active) setError(cause instanceof Error ? cause.message : "No pudimos cargar la rutina."); }); return () => { active = false; }; }, [id]);
  const save = async () => { if (!routine) return; try {
    if (routine.kind === "SHARED") {
      const response = await fetch(`/api/routines/${id}`, { method: "PATCH", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ name: routine.name, type: routine.type, days: routine.days, exercises: routine.exercises }) });
      const body = await response.json().catch(() => ({})); if (!response.ok) throw new Error(body.error || "No se pudieron guardar los cambios.");
    } else await updateRoutineOfflineFirst(id, { name: routine.name, type: routine.type, days: routine.days });
    router.push(routine.kind === "SHARED" ? `/dashboard/community/routines/${id}` : `/dashboard/routine/${id}`);
  } catch (cause) { setError(cause instanceof Error ? cause.message : "No se pudieron guardar los cambios."); } };
  const remove = async () => { if (!window.confirm(routine?.kind === "SHARED" ? "¿Abandonar esta rutina compartida? Tus sesiones se conservan." : "¿Eliminar esta rutina? Esta acción no se puede deshacer.")) return; try { if (routine?.kind === "SHARED") { const response = await fetch(`/api/community/routines/${id}/leave`, { method: "POST" }); const body = await response.json().catch(() => ({})); if (!response.ok) throw new Error(body.error || "No se pudo abandonar la rutina."); router.push("/dashboard/community"); } else { await deleteRoutineOfflineFirst(id); router.push("/dashboard/routine"); } } catch (cause) { setError(cause instanceof Error ? cause.message : "No se pudo eliminar la rutina."); } };
  if (!routine) return <main className="min-h-dvh bg-[#090a08] px-4 pb-36 pt-28 text-white"><div className="mx-auto max-w-2xl text-sm text-white/45">{error || "Cargando rutina…"}</div></main>;
  const updateExercise = (index: number, patch: Partial<Routine["exercises"][number]>) => setRoutine({ ...routine, exercises: routine.exercises.map((exercise, itemIndex) => itemIndex === index ? { ...exercise, ...patch } : exercise) });
  const removeExercise = (index: number) => routine.exercises.length > 1 && setRoutine({ ...routine, exercises: routine.exercises.filter((_, itemIndex) => itemIndex !== index) });
  const addExercise = () => setRoutine({ ...routine, exercises: [...routine.exercises, { id: `new-${Date.now()}`, name: "", muscle: "", sets: 3, reps: 10, weight: 0, technique: "Normal", completed: null, actualReps: null, note: "", trainingDay: routine.days[0] || "Lun" }] });
  const back = routine.kind === "SHARED" ? `/dashboard/community/routines/${id}` : `/dashboard/routine/${id}`;
  return <main className="relative min-h-dvh overflow-hidden bg-[#090a08] px-4 pb-36 pt-28 text-white sm:px-8"><div className="relative mx-auto max-w-2xl"><Link href={back} className="inline-flex items-center gap-2 text-xs text-white/45 hover:text-white"><ArrowLeft size={15}/> Volver a la rutina</Link><header className="mt-8"><p className="text-[10px] font-bold tracking-[.22em] text-[#b7ff00]/70">{routine.kind === "SHARED" ? "RUTINA COMPARTIDA" : "EDICIÓN"}</p><h1 className="mt-2 text-3xl font-semibold tracking-[-.05em]">Ajustá tu rutina.</h1></header><section className="mt-7 rounded-[28px] border border-white/[.08] bg-[#10110e] p-5 sm:p-7"><label className="block"><span className="mb-2 block text-xs text-white/55">Nombre</span><input className="input" maxLength={80} value={routine.name} onChange={(event) => setRoutine({ ...routine, name: event.target.value })}/></label><label className="mt-5 block"><span className="mb-2 block text-xs text-white/55">Tipo de rutina</span><select className="input" value={routine.type} onChange={(event) => setRoutine({ ...routine, type: event.target.value })}><option>Weider</option><option>Torso / Pierna</option><option>Fullbody</option></select></label><div className="mt-5"><p className="mb-2 text-xs text-white/55">Días</p><div className="flex flex-wrap gap-2">{daysOfWeek.map((day) => <button key={day} type="button" onClick={() => setRoutine({ ...routine, days: routine.days.includes(day) ? routine.days.filter((item) => item !== day) : [...routine.days, day] })} className={`rounded-xl px-3 py-2 text-xs font-semibold ${routine.days.includes(day) ? "bg-[#b7ff00] text-black" : "border border-white/10 text-white/45"}`}>{day}</button>)}</div></div>{routine.kind === "SHARED" && <div className="mt-7 border-t border-white/[.07] pt-5"><div className="flex items-center justify-between"><div><p className="text-sm font-semibold">Estructura compartida</p><p className="mt-1 text-xs text-white/35">Los cambios se aplican para ambos; las sesiones pasadas no cambian.</p></div><button type="button" onClick={addExercise} className="inline-flex items-center gap-1 text-xs font-bold text-[#b7ff00]"><Plus size={15}/> Ejercicio</button></div><div className="mt-4 space-y-3">{routine.exercises.map((exercise, index) => <div key={exercise.id} className="rounded-2xl border border-white/[.07] bg-black/20 p-3"><div className="grid gap-2 sm:grid-cols-2"><input className="input" value={exercise.name} maxLength={100} onChange={(event) => updateExercise(index, { name: event.target.value })} placeholder="Ejercicio"/><input className="input" value={exercise.muscle} maxLength={60} onChange={(event) => updateExercise(index, { muscle: event.target.value })} placeholder="Músculo"/><input className="input" type="number" value={exercise.sets} onChange={(event) => updateExercise(index, { sets: Number(event.target.value) })} placeholder="Series"/><input className="input" type="number" value={exercise.reps} onChange={(event) => updateExercise(index, { reps: Number(event.target.value) })} placeholder="Repeticiones"/><input className="input" type="number" value={exercise.weight} onChange={(event) => updateExercise(index, { weight: Number(event.target.value) })} placeholder="Peso kg"/><select className="input" value={exercise.trainingDay} onChange={(event) => updateExercise(index, { trainingDay: event.target.value })}>{routine.days.map((day) => <option key={day}>{day}</option>)}</select></div><div className="mt-2 flex items-center justify-between"><select className="input h-9 max-w-48 text-xs" value={exercise.technique} onChange={(event) => updateExercise(index, { technique: event.target.value })}><option>Normal</option><option>Drop-set</option><option>Rest-pause</option><option>Superserie</option><option>Tempo controlado</option></select><button type="button" disabled={routine.exercises.length === 1} onClick={() => removeExercise(index)} className="text-xs text-red-300/75 disabled:opacity-30"><Trash2 size={15}/></button></div></div>)}</div></div>}{error && <p role="alert" className="mt-5 text-sm text-red-300">{error}</p>}<div className="mt-8 flex items-center justify-between border-t border-white/[.07] pt-5"><button type="button" onClick={() => void remove()} className="flex items-center gap-2 rounded-xl px-3 py-3 text-sm text-red-300/75 hover:bg-red-400/10"><Trash2 size={16}/> Eliminar rutina</button><button type="button" onClick={() => void save()} className="flex items-center gap-2 rounded-xl bg-[#b7ff00] px-5 py-3 text-sm font-bold text-black"><Save size={16}/> Guardar cambios</button></div></section></div></main>;
}
