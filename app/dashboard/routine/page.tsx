"use client";

import Link from "next/link";
import { CircleCheck, Dumbbell, Plus, TrendingUp } from "lucide-react";
import { useEffect, useMemo, useState } from "react";
import { activateRoutineOfflineFirst, getRoutinesOfflineFirst, Routine } from "@/lib/routines";

export default function RoutinePage() {
  const [routines, setRoutines] = useState<Routine[]>([]); const [error, setError] = useState("");
  const load = async () => { const result = await getRoutinesOfflineFirst(setRoutines); setRoutines(result.routines); if (!result.routines.length && result.source === "cache") setError("No hay rutinas guardadas todavía para usar sin conexión."); };
  useEffect(() => { const timer = window.setTimeout(() => { void load(); }); return () => window.clearTimeout(timer); }, []);
  const active = routines.find((routine) => routine.active) || routines[0];
  const complete = active?.exercises.filter((exercise) => exercise.completed).length || 0;
  const volume = useMemo(() => active?.exercises.reduce((total, exercise) => total + exercise.sets * exercise.reps * exercise.weight, 0) || 0, [active]);
  const activate = async (id: string) => { try { const updated = await activateRoutineOfflineFirst(id); setRoutines((items) => items.map((item) => item.id === updated.id ? updated : { ...item, active: false })); } catch (cause) { setError(cause instanceof Error ? cause.message : "No pudimos activar la rutina."); } };
  return <main className="relative min-h-dvh overflow-hidden bg-[#090a08] px-4 pb-36 pt-28 text-white sm:px-8"><div className="pointer-events-none absolute -right-24 top-20 h-80 w-80 rounded-full bg-sky-400/[.09] blur-3xl toro-breathe"/><div className="pointer-events-none absolute -left-24 top-[28rem] h-80 w-80 rounded-full bg-fuchsia-500/[.07] blur-3xl toro-breathe-reverse"/><div className="relative mx-auto max-w-5xl"><header className="flex flex-wrap items-end justify-between gap-4"><div><p className="text-[10px] font-bold tracking-[.22em] text-[#b7ff00]/70">ENTRENAMIENTO</p><h1 className="mt-2 text-4xl font-semibold tracking-[-.06em] sm:text-5xl">Tus rutinas</h1><p className="mt-3 text-sm text-white/40">Planes sincronizados con tu cuenta, estés donde estés.</p></div><Link href="/dashboard/routine/new" className="inline-flex items-center gap-2 rounded-2xl bg-[#b7ff00] px-4 py-3 text-sm font-bold text-black"><Plus size={17}/> Nueva rutina</Link></header>{error && <p role="alert" className="mt-5 rounded-xl border border-red-400/20 bg-red-400/10 p-3 text-sm text-red-200">{error}</p>}{!active ? <Empty/> : <><section className="mt-8 grid gap-3 sm:grid-cols-3"><Metric icon={<Dumbbell size={18}/>} label="Volumen planificado" value={`${Math.round(volume).toLocaleString("es-AR")} kg`} detail={`${active.days.length} días de entrenamiento`}/><Metric icon={<CircleCheck size={18}/>} label="Sesión registrada" value={`${complete}/${active.exercises.length}`} detail="Ejercicios con resultado guardado"/><Metric icon={<TrendingUp size={18}/>} label="Cumplimiento" value={`${Math.round((complete / Math.max(active.exercises.length, 1)) * 100)}%`} detail="Progreso de la rutina activa"/></section><section className="mt-6 overflow-hidden rounded-[28px] border border-[#b7ff00]/15 bg-[#10110e] p-5 sm:p-7"><p className="text-[10px] font-bold tracking-[.2em] text-[#b7ff00]/70">RUTINA ACTIVA</p><div className="mt-2 flex flex-wrap items-end justify-between gap-4"><div><h2 className="text-2xl font-semibold tracking-tight">{active.name}</h2><p className="mt-2 text-sm text-white/40">{active.type} · {active.days.join(", ")} · {active.exercises.length} ejercicios.</p></div><Link href={`/dashboard/routine/${active.id}`} className="rounded-xl border border-white/10 px-4 py-2.5 text-sm font-semibold text-white/70 hover:border-[#b7ff00]/35 hover:text-[#b7ff00]">Abrir sesión</Link></div></section>{routines.length > 1 && <section className="mt-6"><p className="mb-3 text-xs font-bold uppercase tracking-[.18em] text-white/35">Otras rutinas</p><div className="grid gap-3 sm:grid-cols-2">{routines.filter((routine) => routine.id !== active.id).map((routine) => <article key={routine.id} className="rounded-2xl border border-white/[.07] bg-white/[.025] p-4"><p className="font-semibold">{routine.name}</p><p className="mt-1 text-xs text-white/35">{routine.type} · {routine.exercises.length} ejercicios</p><div className="mt-4 flex gap-3"><button onClick={() => void activate(routine.id)} className="text-xs font-bold text-[#b7ff00]">Hacer activa</button><Link href={`/dashboard/routine/${routine.id}`} className="text-xs font-bold text-white/50 hover:text-white">Ver detalle</Link></div></article>)}</div></section>}</>}</div></main>;
}
function Empty() { return <section className="mt-8 rounded-[30px] border border-dashed border-white/10 bg-white/[.02] px-6 py-14 text-center"><div className="mx-auto grid h-14 w-14 place-items-center rounded-2xl bg-[#b7ff00]/10 text-[#b7ff00]"><Dumbbell size={25}/></div><h2 className="mt-5 text-xl font-semibold">Todavía no tenés una rutina.</h2><p className="mx-auto mt-2 max-w-sm text-sm leading-6 text-white/35">Creá un plan, organizá los días y guardá el progreso de cada ejercicio.</p><Link href="/dashboard/routine/new" className="mt-6 inline-flex items-center gap-2 rounded-xl bg-[#b7ff00] px-4 py-3 text-sm font-bold text-black"><Plus size={16}/> Crear rutina</Link></section>; }
function Metric({ icon, label, value, detail }: { icon: React.ReactNode; label: string; value: string; detail: string }) { return <article className="rounded-2xl border border-white/[.07] bg-white/[.025] p-4"><div className="text-[#b7ff00]">{icon}</div><p className="mt-5 text-xl font-semibold">{value}</p><p className="mt-1 text-xs text-white/35">{label}</p><p className="mt-3 border-t border-white/[.06] pt-3 text-[11px] text-white/25">{detail}</p></article>; }
